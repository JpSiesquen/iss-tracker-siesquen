.claude/settings.local.json
.claude/CLAUDE.local.md

# Documento de trabajo interno, no se publica
PLAN.md

# Notas locales, no se publican
ProyectosPotenciales.md

# Notas personales de estudio
Notas/

# --- Generado por Vite ---
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Scripts sueltos para probar algo a mano contra la API o una libreria.
# Necesitan vivir dentro del proyecto para resolver node_modules, pero no
# forman parte de el. El sufijo .scratch.mjs los mantiene fuera del repo
# conservando la extension que Node necesita para saber que son ES modules.
*.scratch.mjs
